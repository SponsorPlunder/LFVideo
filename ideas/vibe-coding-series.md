# 系列一《Vibe Coding 造一条自动化视频生产线》分期规划

> 围绕本项目真实的架构、工作流、角色与技术选型，做成一个系列；每期 3–10 分钟。
> 组织原则：**边做边播（Dogfooding）**——每期 = 把流水线某个能力做到"能用/更好用"的一个里程碑，同时把这个过程拍成内容。
> 真实进度以 [`content-library/PIPELINE.md`](../content-library/PIPELINE.md) 看板为唯一真相；本文件是脑暴/细化稿。

---

## 主标题

**《Vibe Coding 造一条自动化视频生产线》**

呈现格式：主标题 + 每期短标题，例如「Vibe Coding 造视频生产线 · EP02 代码即视频」。

> 判断层提醒：纯 "Vibe Coding" 给人"全靠感觉、不读代码"的印象，但本项目有 Schema 校验门 + 角色体系兜底——把"Vibe 也要有护栏"当成卖点，不做真·裸奔。

---

## 三条贯穿全系列的原则

1. **每期一个能力闭环**：一期只讲透一块（渲染 / 字幕 / 声音 / 状态机 / 角色 / 选型），先把它在仓库里做到可用，再拍。
2. **操作层 + 判断层**：操作层=照着能复现的步骤/配置/命令；判断层=边界与验收、哪一步要人接管、怎么验收。如实但不夸大（反噱头）。
3. **内容即证据**：成片本身就是用这条流水线产出的——以行证言。

---

## 分期总览

| 期 | 短标题 | 一句话核心问题 | 时长 | 轨 | 对应代码模块 | 本期同步要完善的自动化/质量点 |
|----|--------|---------------|------|----|-------------|------------------------------|
| **ep01-video-agent-overview** | 把视频写成代码 | 一个内容频道为什么要把自己的视频生产线代码化？ | 5–6′ | A | 全局架构 / `Explainer` 契约 | 渲染规范 1080p、配置契约定稿（已完结，可校准重制） |
| **ep02-video-render** | 代码即视频 | 一套 React 场景组件怎么把"参数"编译成成片？ | 6–8′ | A | `remotion-composer`（`cut.type`/`overlay`、charts、`terminal_scene`/`screenshot_scene`、`VRMAvatar`） | 场景组件补全 + VRM 主持人站姿/口型质量（**本期**） |
| **ep03-video-subtitle** | 让字幕踩着话音跳 | 毫秒级字级时间戳怎么驱动弹跳字幕和口型？ | 4–6′ | B | `tools/analysis/transcriber`（WhisperX）→ 字幕烧录 | 字级对齐链路打通、术语校对词典、口型韵母映射 |
| **ep04-video-audio** | 会让路的背景音乐 | 人声和 BGM 怎么自动"此消彼长"、各平台响度怎么对齐？ | 3–5′ | A/B | `tools/audio/audio_mixer`（智能闪避）、`audio_probe` | 自动 ducking + LUFS 响度标准化链路 |
| **ep05-video-pipeline** | 流程也是代码 | 一条内容流水线怎么变成"可断点续跑的状态机"？ | 5–7′ | A | `.devin/workflows/*`、`PIPELINE.md`、`scripts/pipeline_lint.py`、`shared/schemas/*` | 增强 `pipeline_lint` 防漂移、扩大 Schema 覆盖阶段 |
| **ep06-video-orchestrator** | 给 AI 发工牌干活 | Markdown 角色库怎么变成多智能体自动流转？ | 5–8′ | A | `shared/roles/*`、`OpenMontage/pipeline_defs/*.yaml` | 角色库 → YAML 管道编排打样（迈向 L1 半自动） |
| **ep07-video-tech-choices** | 我踩过的选型坑 | 同样的目标，为什么选 Remotion / AGPL OpenMontage / 拒绝数字人对口型？ | 5–7′ | A | `_decisions/*`、`meta/judgment-layer` | 决策记录沉淀、护城河（反噱头门）梳理 |

> ep03 与 ep04 体量都不大，如果想压缩节奏，可合并为一期「声音与字幕」（6–8′）。ep05+ep06 是"流程即代码"的上下篇，建议保持两期。

---

## 每期展开

### ep01 · 把视频写成代码（总览）
- **核心问题**：为什么把整条视频生产线开源化、代码化？这套"IDE 智能体 + Python 工具箱 + React 编译器"三层架构是什么。
- **操作层**：跑一遍最小成片 demo——一份 JSON 配置 → `npx remotion render` → 出片，展示"改一个参数，画面就变"。
- **判断层**：代码化的代价（学习曲线、调试在浏览器里、不适合每期结构都大改的内容）；什么内容**不**该这么做。
- **成片要点**：架构图（`diagram` → Remotion 平滑变焦）、母题金句"视频不是画面堆砌，是参数输入与场景渲染"。

### ep02 · 代码即视频（渲染引擎，本期）
- **核心问题**：`remotion-composer` 怎么用 `cut.type`/`overlay.type` 把一份配置分发成不同场景。
- **操作层**：演示对比卡 `comparison`、图表 `bar/line/pie/kpi`、终端模拟 `terminal_scene`、截图叠加 `screenshot_scene`、VRM 主持人取景预设。
- **判断层**：**合成录屏的边界**——15–30s 聚焦演示能以假乱真，复杂连续交互会穿帮，必须切真实录屏；数字主持人适合"陪衬"不适合"主角"。
- **同步完善**：补齐常用场景组件；把 VRM 脚部站姿 + 口型纳入质量基线。

### ep03 · 让字幕踩着话音跳（字幕卡点）
- **核心问题**：WhisperX 的字级时间戳怎么变成踩点弹跳字幕，并驱动口型。
- **操作层**：真人口播音频 → `transcriber` 出字级 JSON → Remotion 烧录字级高亮/弹跳；中文分词 + 拼音韵母 → VRM 口型。
- **判断层**：ASR 在专业术语/中英混读上的错字率，需要人工校对 + 术语词典；长句对齐漂移怎么修。
- **成片要点**：本期就是 B 轨，字幕效果即"现场证据"。

### ep04 · 会让路的背景音乐（声音工程）
- **核心问题**：人声与 BGM 如何自动闪避（ducking）、多平台响度如何统一。
- **操作层**：`audio_mixer` 算法级闪避 + 响度标准化；`audio_probe` 找气口对齐转场。
- **判断层**：自动 ducking 的失灵场景（音乐高潮压住人声）、各平台 LUFS 差异、什么时候必须手动调。
- **成片要点**：A/B 波形可视化对比"开/关 ducking"。

### ep05 · 流程也是代码（13 阶段状态机）
- **核心问题**：怎么把"选题→…→归档"这条流水线变成一台可断点续跑、防漂移的状态机。
- **操作层**：`PIPELINE.md` 看板 + frontmatter 状态；`pipeline_lint.py` 跑 Schema 契约 + 跨阶段防漂移门禁；演示"看板续跑"。
- **判断层**：纯人审 L0.5 的瓶颈；Schema 约束太死 vs 太松；哪些该机器卡、哪些必须人卡。
- **同步完善**：增强 lint 防漂移规则、把 Schema 覆盖到更多阶段。

### ep06 · 给 AI 发工牌干活（角色即 Prompt → 编排）
- **核心问题**：`shared/roles/` 的 Markdown 角色库，怎么从"手动复制 Prompt"走到"多智能体自动流转"。
- **操作层**：角色=system prompt、工作流=user prompt、状态机=调度；用 `pipeline_defs/*.yaml` 串一条可执行管道打样。
- **判断层**：角色膨胀与互相打架、上下文衰减、何时该合并角色；从 IDE 交互到 API 化的真正难点（反馈回路、B 轨需人上传素材的"挂起等待"）。
- **同步完善**：跑通一条角色驱动的半自动管道（迈向 L1）。

### ep07 · 我踩过的选型坑（技术选型复盘）
- **核心问题**：同样的目标，为什么是 Remotion 而非 HyperFrames？为什么容忍 AGPL 的 OpenMontage？为什么坚决不做数字人对口型 / AI 绘界面？
- **操作层**：把 `_decisions/` 的决策树讲清楚，配反例对比。
- **判断层**：每个选型的代价与"后悔点"，以及护城河（反噱头 / 判断层评审门）为什么重要。
- **成片要点**：决策树动画 + "如果当初选了另一个会怎样"的对照。

---

## 建议的制作顺序与节奏

1. **先收尾 ep02**（本期已在 08 字幕 draft），把渲染引擎 + VRM 质量打成系列的"画面基线"。
2. **ep03 → ep04**：字幕、声音是所有后续期的通用基础设施，先做完，后面每期都直接受益。
3. **ep05 → ep06**：流程/角色是"上下篇"，做这两期时顺便把半自动（L1）推上去。
4. **ep07 复盘**收尾，沉淀决策，也为"系列二 PPT 自动化"做钩子。
- 节奏：每周 1 期、单期投入 ≤8h；完成度 > 完美度，宁可粗糙也不停更。
